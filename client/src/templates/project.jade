.project
  .sidebar
    .project-management
      .project-name Loading...
      .project-buttons
        button(title="Export game",disabled).export
        button(title="Debug game (F6)",disabled).debug
        button(title="Run game (F5)",disabled).run

    .entries-buttons
      button(title="New asset",disabled).new-asset
      button(title="New folder",disabled).new-folder
      // button(disabled).open-entry.single.asset-only Open
      button(title="Rename",disabled).rename-entry.single.edit
      button(title="Duplicate",disabled).duplicate-entry.single.asset-only.edit
      button(title="Trash",disabled).trash-entry.edit
      button(title="Search (Ctrl+O)",disabled).search
    .entries-tree-view
      .connecting
        div Connecting...
    .tools
      ul
  .main
    .top
      .tabs-bar
      .controls
        button.toggle-notifications
        button.minimize
        button.maximize
        button.close
    .panes
