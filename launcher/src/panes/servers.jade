.buttons.button-strip
  button.add-server Add server
  button.rename-server Rename
  button.edit-address Edit address
  button.remove-server Remove server
.servers-tree-view

.my-server
  header
    | My Server 
    .buttons
      button.start-stop-server Start
    .auto-start-server
      input#auto-start-server(type="checkbox")
      label(for="auto-start-server") Auto-start server on startup
  textarea(readonly)
